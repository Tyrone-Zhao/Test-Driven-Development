
./lists/static/tests/list.js
 #1 'window' was used before it was defined.
    window.Superlists = {}; // Line 1, Pos 1
 #2 'window' was used before it was defined.
    window.Superlists.updateItems = function (url) { // Line 3, Pos 1
 #3 Missing 'use strict' statement.
    $.get(url).done(function (response) { // Line 4, Pos 5
 #4 Combine this with the previous 'var' statement.
    var i; // Line 6, Pos 13
 #5 Missing space between 'i' and '='.
    for (i=0; i<response.length; i += 1) { // Line 7, Pos 15
 #6 Missing space between '=' and '0'.
    for (i=0; i<response.length; i += 1) { // Line 7, Pos 16
 #7 Missing space between 'i' and '<'.
    for (i=0; i<response.length; i += 1) { // Line 7, Pos 20
 #8 Missing space between '<' and 'response'.
    for (i=0; i<response.length; i += 1) { // Line 7, Pos 21
 #9 Don't declare variables in a loop.
    var item = response[i]; // Line 8, Pos 17
#10 Missing space between 'i' and '+'.
    rows += "\n<tr><td>" + (i+1) + ": " + item.text + "</td></tr>"; // Line 9, Pos 38
#11 Missing space between '+' and '1'.
    rows += "\n<tr><td>" + (i+1) + ": " + item.text + "</td></tr>"; // Line 9, Pos 39
#12 '$' was used before it was defined.
    $("#id_list_table").html(rows); // Line 11, Pos 9
#13 Unexpected 'window'.
    window.Superlists.initialize = function (url) { // Line 15, Pos 1
#14 Stopping. (35% scanned).
     // Line 15, Pos 1
